npm i  when we run this command automatically nodemodules and package.json and package-lock.json files were created. without using react app

when we modify values we have restart server in jsonserver
by using nodemon we can automatically restart the server npm i -g nodemon


npx create-react-app Ui
Cannot create a project named "Ui" because of npm naming restrictions:

  * name can no longer contain capital letters

Please choose a different project name.
